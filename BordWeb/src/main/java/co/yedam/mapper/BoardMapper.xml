<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="co.yedam.mapper.BoardMapper">
	<select id="boardList" resultType="co.yedam.vo.BoardVO">
		select *
		from board_tbl
		order by board_no
	</select>

	<insert id="insertBoard" parameterType="co.yedam.vo.BoardVO">
		insert into
		board_tbl (board_no, title, content, writer)
		values(#{stdNo},#{stdName},#{phone},#{bldType})
	</insert>

	<update id="updateBoard" parameterType="co.yedam.vo.BoardVO">
		update tbl_student
		set phone = #{phone}
		where std_no = #{stdNo}
	</update>
	
	<select id="getBoard" parameterType="string" resultType="co.yedam.vo.BoardVO">
		select *
		from tbl_student
		where std_no = #{stdNo}
	</select>
	
	
	

</mapper>